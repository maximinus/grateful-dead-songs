
This folder contains backups of the DB in the SQL format.

The directory is used to save the database from the web-site.

This folder must be present in the local and server versions, however the
backups are done from a CRON job on the server.
