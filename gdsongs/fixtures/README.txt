
This folder contains the data needed to fill a database for testing

Reference this file as

class MyTestClass(TestCase):
	fixtures = ['fixtures/file_in_this_folder.json']

The data can be generated by the command

	./manage.py dumpdata > my_filename.json
